import { changePassword, getProfile, updateProfile, userList, userDetails, deleteUser } from "./user.controller";
const user = {
	changePassword,
	getProfile,
	updateProfile,
	userList,
	userDetails,
	deleteUser, 
};
export default user;
